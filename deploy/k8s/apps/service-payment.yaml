apiVersion: v1
kind: Service
metadata:
  name: payment
  namespace: espx
  labels:
    app.kubernetes.io/name: payment
    app.kubernetes.io/part-of: espx
spec:
  selector:
    app.kubernetes.io/name: control
  ports:
    - name: webhook
      port: 8187
      targetPort: webhook

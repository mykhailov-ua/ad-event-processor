# Load-test stack: optional /click and /openrtb/bid routes disabled (main nginx.load-test.conf serves /track).
# Full snippet uses limit_except { return 405; } which OpenResty rejects; production snippet fixed separately.


services:
  payment:
    environment:
      - OPS_ALERTS_ENABLED=${OPS_ALERTS_ENABLED:-true}
      - DB_DSN=${DB_DSN}
      - PAYMENT_FINANCIAL_RECON_INTERVAL_MS=3600000

  management:
    environment:
      - OPS_ALERTS_ENABLED=${OPS_ALERTS_ENABLED:-true}

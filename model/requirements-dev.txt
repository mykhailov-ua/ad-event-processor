# Dev/type-check tooling (install after requirements.txt).
# Bootstrap: bash scripts/dev/model_venv.sh
# Format: bash scripts/dev/model_format.sh
# Then select interpreter: model/.venv/bin/python (Cursor Python: Select Interpreter).
-r requirements.txt
pyright[nodejs]>=1.1.0

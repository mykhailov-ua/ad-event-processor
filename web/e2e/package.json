{
  "name": "ad-event-processor-admin-e2e",
  "private": true,
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:m8": "playwright test cpa_held_out.spec.js --grep 'ops consolidation' && playwright test ops_compliance.spec.js"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.1",
    "@playwright/test": "^1.51.0"
  }
}

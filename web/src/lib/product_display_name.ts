/** Operator-visible product title. Wire/env slug stays ad-event-processor. */
export const productDisplayName = 'Ad Event Processor';

export function productConsoleTagline(): string {
  return `${productDisplayName} operator console`;
}

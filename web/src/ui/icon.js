/**
 * Icons disabled — structure-only admin UI. Call sites unchanged; no SVG output.
 *
 * @param {string} _name
 * @param {{ size?: number, className?: string, strokeWidth?: number }} [_opts]
 * @returns {null}
 */
export function renderIcon(_name, _opts = {}) {
  return null;
}

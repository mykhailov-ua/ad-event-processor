[tool.ruff]
target-version = "py312"
line-length = 120

[tool.ruff.lint.isort]
known-first-party = [
    "ch_client",
    "feature_spec",
    "labeled_dataset",
    "policy_calibrate",
    "policy_config",
    "scoring_policy",
    "shadow_precision",
    "traffic_simulator",
]

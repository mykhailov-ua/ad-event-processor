name: Sentinel Resilience

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: sentinel-${{ github.ref }}
  cancel-in-progress: true

jobs:
  sentinel-failover:
    name: Redis Sentinel failover resilience
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: '1.25.12'
          cache: true

      - name: Prepare env
        run: bash scripts/fault/sentinel_failover_env.sh

      - name: Sentinel failover resilience test
        run: bash scripts/lib/tee_ci_log.sh sentinel.log bash scripts/test/sentinel.sh

      - name: Upload sentinel log
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: sentinel-log
          path: var/ci/sentinel.log
          retention-days: 7

---
description: SLA, sources-only repo, rule index, commits, backlog routing
alwaysApply: true
---

Human docs: `docs/ARCHITECTURE.md`, `docs/DEVELOPMENT.md`. Agent rules: `.cursor/rules/*.mdc`. Open backlog: repo-root `BACKLOG.md` — do not duplicate in rules or docs.

Engineering rules live in `.cursor/rules/*.mdc`. Do not duplicate agent rules into `docs/`.

SLA and budgets

- Tracker `ad_http_request_duration_seconds`: p95 < 50 ms, p99 < 80 ms, ceiling 100 ms.
- Production `FILTER_TIMEOUT_MS` <= 100; dev default 5000 ms.
- Redis unified-filter Lua p99 < 10 ms per shard.
- Geo filter p99 < 10 us (sampled).
- `RunAuction` p99 < 15 us; p99 candidates scanned < 500.
- Fraud boost snapshot: incremental < 500 us per `FilterEngine.Check`; `BenchmarkFilterFraudBoost` ~90 ns, 0 allocs/op.
- `GetShard` (StaticSlot): ~5.6 ns, 0 allocs/op.
- Load-test abort: control-cohort p99 > 80 ms for 30 s or budget invariant violation.
- Budget invariant: `current_spend <= budget_limit` in Postgres; spend tests use `AssertBudgetInvariant` (+/-1 micro-unit).

Sources-only repo

Fresh clone is not runnable out of the box: run `make gen` / `make proto`, provide env/secrets, build BPF, fetch GeoIP, start compose. Do not commit generated code, `bin/*`, `.cache/`, `var/`, secrets, or runtime JWT files. Canonical ignore: root `.gitignore`. Local dev license path: `var/license.jwt` (not repo root `license.jwt`). Root directory is NOT a junk drawer: no `*.txt` or `*.log` (except standard project files); use `var/` or `data/` instead.

Commit policy

- Title: imperative, plain English; lead with Add, Remove, Fix, Wire, Drop, Guard, Reject; name concrete surface (route, handler, table, env var, binary).
- Avoid: `refactor:`, `docs:`, `chore:`, vague umbrellas (`perf`, `cleanup`, `misc`, `WIP`), milestone tokens (`M-…`, `GAP-…`).
- Body optional: why and what to verify; no file manifest.
- One logical change set per commit; no docs-only commits unless code ships in the same commit.

Rules (flat `.cursor/rules/`)

Always on: `core.mdc`, `quality.mdc`, `anti-slop.mdc`, `naming.mdc`.

| Rule | Topic |
| :--- | :--- |
| `architecture.mdc` | Hot/cold invariants, I/O round-trip limits, fail-closed |
| `structure.mdc` | Current flat layout, cmd map, future split backlog |
| `development.mdc` | Codegen, compose, benchmarks, fault runbooks |
| `ci.mdc` | Merge gates, BPF thresholds, test tiers |
| `code-style.mdc` | Layout, types, sqlc, proto, PR gates |
| `hot-path.mdc` | `/track`, parsers, gnet, filters, 0 allocs |
| `parser.mdc` | Ingress wire policy, chaos verification |
| `traffic.mdc` | `/click`, `/track`, macros, CAPI |
| `cold-path.mdc` | Admin, payment, ledger, notify |
| `data-layer.mdc` | Redis, Lua, PG, CH, sharding |
| `boundaries.mdc` | `cmd/*` ownership and imports |
| `control-plane.mdc` | Outbox, workers, admin API |
| `rtb.mdc` | OpenRTB auction, shadow→live |
| `edge.mdc` | Nginx Lua, XDP, tracker parity |
| `compliance.mdc` | Defensive perimeter |
| `licensing.mdc` | JWT, trial registry, USDT tiers |
| `regions.mdc` | Enterprise multi-region |
| `ui.mdc` | Admin React design system |
| `tradeoffs.mdc` | Rejected alternatives, evidence matrix |
| `fault-tests.mdc` | Chaos/outbox/parser proofs |
| `testing.mdc` | Test taxonomy, holdouts |
| `load-test-bpf.mdc` | Load test + BPF probes |

Precedence: `core.mdc` on SLA and repo policy; `anti-slop.mdc` on CI honesty; `testing.mdc` on holdouts; `hot-path.mdc` on ingest; `cold-path.mdc` on admin/payment; `structure.mdc` on current layout (flat controlplane canonical).

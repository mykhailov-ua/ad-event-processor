---
description: Naming, comments, forbidden legacy tokens - always applied on every edit
alwaysApply: true
---

# Naming and Comments

## Purpose

Always applied on every agent turn and every file edit. Forbid cryptic identifiers and legacy product tokens. Enforce semantic clarity aligned with `lead-intent-processor`. Comment discipline: explain **why**, not what the name already says.

Guard script: `bash scripts/ci/check_no_legacy_naming.sh`. Go receivers and web JSDoc: `quality.mdc`. Package layout and banned subtrees: `structure.mdc`. Root clutter: `core.mdc` Sources-only repository.

## Forbidden trash naming

Do not use cryptic suffixes, meaningless numbers, or lazy abbreviations.

| Pattern | Why banned | Better alternative |
| :--- | :--- | :--- |
| `m1`, `m2`, `m8` | Cryptic milestone/versioning | `load_test_milestone_8`, `v2_reconciler` |
| `n1`, `n2` | Meaningless numeric tags | `ledger_fix_2026`, `consistency_patch` |
| `v1_old`, `new_logic` | Temporal naming (rots immediately) | `legacy_v1_compat`, `batch_processor_v2` |
| `f1`, `f2` | Lazy filter/field abbreviations | `license_filter`, `geo_filter` |
| `ch`, `pg`, `rd`, `rdb`, `rdbs` | Driver/client abbreviations | `clickhouseConn`, `postgresConn`, `redisClient`, `redisShards` |
| `res`, `res2` | Generic result variables | `auction_result`, `auth_response` |
| `GAP_prefix_001` style codes, `WI-123` | Bureaucratic ticket/milestone IDs | Semantic slug: `wire_silent_reject_hot_path` |
| `M-8`, `M8` milestone tokens in titles | Milestone tokens in commits and headings | Concrete surface name: `silent_reject_hot_path` |

## Forbidden bureaucratic naming

Do not use ticket prefixes, gap codes, or milestone IDs in file names, backlog headings, commit titles, operator docs, or Grafana panel text.

| Banned | Use instead |
| :--- | :--- |
| Ticket-style gap codes (`GAP` + team + number) | Describe the work: `antifraud_doc_code_parity` |
| `P0 item #4`, `Action item 7` | Same semantic slug in `deploy/vendor/antifraud_backlog.md` |
| `WI-4521`, `JIRA-123` | Link to tracker URL in PR description only; not in repo prose |

Backlog files: one `### semantic_slug` per item; cross-reference by slug (`after silent_reject_rename_end_to_end`), not numeric codes.

CI: `bash scripts/ci/check_no_legacy_naming.sh` rejects `GAP-<TEAM>-<digits>` in `README.md`, `docs/`, `deploy/vendor/`, `.cursor/rules/`.

Exception: `fault_proof gap=open` in fault tests is telemetry, not a backlog ID (`fault-tests.mdc`).

## Legacy token ban

Strictly forbidden in new code, documentation, and variable names. Remove existing occurrences when touched.

| Forbidden | Use instead |
| :--- | :--- |
| `BidShard` | `ad-event-processor` |
| `espx`, `espx_`, `ESPX_` | `ad-event` or `ad_event` |
| `legacy`, `old` (without context) | Specific context, e.g. `legacy_v1_api` |

## Product vs stack surfaces

| Surface | Canonical name |
| :--- | :--- |
| Go module, imports, internal docs | `ad-event-processor` |
| Docker compose volumes | `ad_event_processor_*` (historical prefix) |
| Admin UI product strings | `ad-event-processor` |
| Control plane API | `/api/v1/*` on `:8188` |

## Forbidden marketing prose

Technical docs (`README.md`, `docs/`, `deploy/vendor/ANTIFRAUD.md`, `docs/ARCHITECTURE.md`, Grafana descriptions) must not read like sales copy. Doc lie catalog: `anti-slop.mdc` Documentation lie modes.

| Banned in tech docs | Use instead |
| :--- | :--- |
| `enterprise-grade`, `premium`, `world-class` | Name the surface: `edge-xdp`, `FilterEngine` |
| `ultra-low latency`, `ultra-fast`, `lightning-fast` | SLA from `core.mdc` or bench name + scope |
| `guarantee`, `guaranteed`, `never` (unqualified) | Concrete behavior or `fail-closed` / batch-only ML |
| `eliminated` (Redis/script ops) | File + function that runs the op, or "cached on miss" |
| `hundreds of thousands of RPS` (unqualified) | Load-test config or measured metric |
| `Why buyers choose`, competitor tables | Remove; SKU limits in `sku.yaml` only |
| `unlimited` as hype | License field `max_active_campaigns: 0` = no cap in JWT schema |
| `intelligent`, `AI-powered` (unqualified) | `cmd/fraud-scorer`, `cmd/ivt-detector`, SKU flag |
| `complete data privacy`, `spy-tool` | Omit; not verifiable in code |
| `crush`, `eliminates fees` pitch | Omit |

`deploy/vendor/SALES.md`: SKU table and license workflow only; no competitor anchors or positioning essays.

User-visible admin UI (`web/src`): no `(skeleton)`, demo KPIs, or "coming soon" without `StubBanner` (`ui.mdc`, `anti-slop.mdc`).

## Semantic clarity

Match naming quality and structure of `lead-intent-processor`.

### Structs and fields

- **No god structs:** avoid `Service` structs with 50+ unrelated fields. Split by domain role or worker.
- **Explicit roles:** `PostgresGate` is too vague. Use `PostgresWriteGate` or `PostgresCampaignStore`.
- **Field names:** `settlePoolField` is trash. Use `SettlementPool`.

### Handler and interface names

Banned as interface or field names on HTTP handlers: `API`, `Backend`, `Provider`, `Service` (when the name only means "implementation"). Name the **route or domain role** (`MiniAppRoutes`, `CampaignReader`). File matches type: `mini_app_routes.go`, not `backend.go`.

### File and path naming

- Production Go: snake_case `<domain>_<rest>.go` - `track_core.go`, `service_campaigns.go`.
- Split near 500 lines or when transport mixes with core logic.
- Tests beside code: `*_test.go`, `*_bench_test.go`, `*_fault_test.go`.

```go
// BAD path
internal/controlplane/service_campaign_m8.go

// GOOD path
internal/campaign/load_test_worker.go
```

## Examples

```go
// BAD
f := openFilter()
v1 := f.Check(req)

// GOOD
geoFilter := openGeoFilter()
verdict := geoFilter.Check(request)

// BAD
type Service struct {
    ch driver.Conn
    m8 *Worker
}

// GOOD
type ClickHouseStore struct {
    conn           driver.Conn
    loadTestWorker *LoadTestWorker
}
```

## Comments - tone and format

| Rule | Detail |
| :--- | :--- |
| Quantity | One comment per constraint or decision, not per statement |
| Tone | Imperative ("Return...", "Mount..."). Dry, factual prose |
| Charset | ASCII only (0x20-0x7E) |
| Config fields | Document units, SLA ceilings, security boundaries |
| Financial / SLA | Call out invariants at I/O boundaries (budget, debit, outbox, webhook ACK) |

## Comments - forbidden decoration

Banned in code comments, doc comments, commit messages, and `.cursor/rules` prose:

| Pattern | Examples | Use instead |
| :--- | :--- | :--- |
| Line banners | `// ====`, `// ----`, `// ****`, `/* ===== */` | Blank line or one imperative sentence |
| Section labels in comments | `// --- Helpers ---`, `// ### Section` | Name the function or drop the banner |
| Unicode dashes | em dash, en dash, figure dash | ASCII `-` or rewrite with `:`, `;`, comma |
| Ellipsis character | single-char ellipsis | `...` (three ASCII dots) if needed |
| Emojis and symbols | checkmarks, warning icons, decorative bullets | Plain words: `FAIL`, `WARN`, `NOTE` |
| Box drawing | `+---+`, pipe tables inside `//` blocks | Markdown in docs only; not in Go comments |
| Script/log banners | `echo "== step =="`, `log "--- section ---"`, `=== title ===` in `scripts/` | Plain `echo "gate_name: step"` or `log "step"` |

YAML frontmatter `---` in `.mdc` files is required metadata, not a comment banner.

`.cursor/rules` tables use markdown `| :--- |` row syntax only; not repeated `---` or `===` separator lines in body text.

## Comments - do not comment

- What the name already says (`// List bots`, `// return error`).
- Every struct field, every import group, every branch.
- Package doc that only repeats the directory name.
- Interface or type doc that duplicates the type name without contract.
- Change logs, author names, or ticket URLs in code (use git).

## Comments - when required

Add a single imperative line only where code cannot express **why** or **constraint**:

| Topic | Example |
| :--- | :--- |
| Wire/schema | External pubkey, protobuf field semantics, Redis hash tag for cluster slot |
| Best-effort path | Error intentionally not returned; downstream must still run |
| SLA / budget | Webhook ACK ceiling, rate limit matching vendor API |
| Security | Trust anchor, fail-closed boundary, idempotency key scope |
| Invariant | Financial balance, ordering, at-least-once vs exactly-once |
| Exported API | Concurrency, error semantics, nil infra behavior when non-obvious |

Best-effort error handling: either return the error, or one comment why propagation would break a stronger invariant (never silent `_ =` without one of these - see `anti-slop.mdc`).

### Exported symbols

- Exported func/type: doc comment when errors, nil inputs, or concurrency are not obvious from the signature.
- Exported cold-path handlers: document body limits and auth expectations only when not enforced in code at call site.
- Hot path (`hot-path.mdc`): at most one comment per non-obvious branch; no narrative.

### Tests

- No comments on table-driven row labels unless the case name is insufficient for a failing holdout.
- Fault tests may log `fault_proof ...`; that is telemetry, not a substitute for assertions.

## Related rules

`anti-slop.mdc`, `code-style.mdc`, `core.mdc`, `quality.mdc`, `structure.mdc`.
